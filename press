#!/usr/bin/env php
<?php
if (file_exists(__DIR__.'/../../autoload.php')) {
    require __DIR__.'/../../autoload.php';
} else {
    require __DIR__.'/vendor/autoload.php';
}

use KindlingCLI\Command\NewCommand;
use KindlingCLI\Command\InstallCommand;
use KindlingCLI\Command\ConfigureCommand;
use Symfony\Component\Console\Application;

if (file_exists(__DIR__.'/../../bin/wp')) {
    define('PRESS_CLI_WP_EXEC', __DIR__.'/../../bin/wp');
} else {
    define('PRESS_CLI_WP_EXEC', __DIR__.'/bin/wp');
}

define('PRESS_CLI_EXEC_DIR', __DIR__);

putenv('WP_CLI_CONFIG_PATH='.PRESS_CLI_EXEC_DIR . '/config.yml');

$application = new Application();

$application->add(new InstallCommand);
$application->add(new ConfigureCommand);
$application->add(new NewCommand);

$application->run();
